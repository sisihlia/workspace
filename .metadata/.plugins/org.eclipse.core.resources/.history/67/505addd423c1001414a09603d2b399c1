package test;
import com.mongodb.BasicDBObject;
import com.mongodb.BulkWriteOperation;
import com.mongodb.BulkWriteResult;
import com.mongodb.Cursor;
import com.mongodb.DB;
import com.mongodb.Mongo;
import com.mongodb.DBCollection;
import com.mongodb.DBCursor;
import com.mongodb.DBObject;
import com.mongodb.MongoClient;
import com.mongodb.ParallelScanOptions;

import java.util.Collections;
import java.util.List;
import java.util.Set;

import javax.swing.text.Document;

import static java.util.concurrent.TimeUnit.SECONDS;
public class mongodb
{
	
	private static void selectSingleRecordAndFieldByRecordNumber(DBCollection collection) {
	    BasicDBObject whereQuery = new BasicDBObject();
	    whereQuery.put("item", 3);
	  
	    DBCursor cursor = collection.find(whereQuery, whereQuery);
	   
	        System.out.println(cursor.toString());
	        String[] s= cursor.toString().split("*item*");
	        System.out.println(s);
	        
	    
	}
	
	public static void main(String[] args){
		   try{   // get the collection name in indicated database
			   MongoClient mongo = new MongoClient("localhost", 27017);
			  
			DB db = mongo.getDB("mydb");
			 DBCollection collection = db.getCollection("mydbColl");
			 selectSingleRecordAndFieldByRecordNumber(collection);
			  /*// DBCollection collection = collection.find();
			    		//Document item = ((Object) collection.find()).first();
		//DBCursor item = collection.find();
			   //System.out.println(item.toString()); // [datas, names, system.indexes, users]
			     */ }catch(Exception e){
				     System.err.println( e.getClass().getName() + ": " + e.getMessage() );
				  }
			   }
			}